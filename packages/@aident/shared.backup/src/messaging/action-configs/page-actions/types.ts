export enum InteractableInteraction {
  CLICK = 'click',
  FOCUS = 'focus',
  HOVER = 'hover',
  // KEY_DOWN = 'key-down',
  // KEY_UP = 'key-up',
  PRESS_KEY = 'press-key',
  SAVE_CANVAS_AS_PNG = 'save-canvas-as-png',
  SCROLL = 'scroll',
  SCROLL_INTO_VIEW = 'scroll-into-view',
  // SELECT = 'select',
  TAP = 'tap',
  TYPE = 'type',
  WAIT = 'wait',
}

export enum PageNavigationAction {
  GO_BACK = 'go-back',
  GO_FORWARD = 'go-forward',
  GOTO = 'goto',
  RELOAD = 'reload',
}
